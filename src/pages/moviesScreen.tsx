import { Layout } from "../components/layout"
import { Movies } from "../components/movies"

export const MoviesScreen=()=>{
    return(
        <Layout>
            <Movies/>
        </Layout>
    )
}